@model ICollection<PurchDep.Domain.Product>

<section>
    <h1>Products List</h1>
    <ul>
        @foreach (var product in Model!)
        {
            <li>
                <a asp-action="Details" asp-route-id="@product.Id">@product.Name</a>
            </li>
        }
    </ul>
</section>